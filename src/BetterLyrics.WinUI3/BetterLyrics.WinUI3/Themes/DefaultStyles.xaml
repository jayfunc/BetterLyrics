<?xml version="1.0" encoding="utf-8" ?>

<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:uc="using:BetterLyrics.WinUI3.Controls">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Themes/DefaultAccentSplitButtonStyle.xaml" />
        <ResourceDictionary Source="/Themes/DefaultListViewItemStyle.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--  Button Styles  -->
    <Style
        x:Key="TitleBarButtonStyle"
        BasedOn="{StaticResource DefaultButtonStyle}"
        TargetType="Button">
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="16,9,16,9" />
        <Setter Property="Background" Value="Transparent" />
    </Style>
    <Style
        x:Key="GhostButtonStyle"
        BasedOn="{StaticResource DefaultButtonStyle}"
        TargetType="Button">
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="Transparent" />
    </Style>

    <!--  ToggleButton Styles  -->
    <Style
        x:Key="TitleBarToggleButtonStyle"
        BasedOn="{StaticResource DefaultToggleButtonStyle}"
        TargetType="ToggleButton">
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="14,6,14,9" />
        <Setter Property="Background" Value="Transparent" />
    </Style>
    <Style
        x:Key="GhostToggleButtonStyle"
        BasedOn="{StaticResource DefaultToggleButtonStyle}"
        TargetType="ToggleButton">
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="5,5,5,6" />
        <Setter Property="Background" Value="Transparent" />
    </Style>

    <!--  SplitButton Styles  -->
    <Style
        x:Key="AccentSplitButtonStyle"
        BasedOn="{StaticResource DefaultAccentSplitButtonStyle}"
        TargetType="SplitButton" />

    <!--  ExternalLinkButton Styles  -->
    <Style BasedOn="{StaticResource DefaultHyperlinkButtonStyle}" TargetType="uc:ExternalLinkButton" />

    <!--  ListViewItem Styles  -->
    <Style BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem" />
    <Style
        x:Key="ListViewStretchedItemContainerStyle"
        BasedOn="{StaticResource DefaultListViewItemStyle}"
        TargetType="ListViewItem">
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Padding" Value="0" />
    </Style>
    <Style
        x:Key="ListViewCardStyle"
        BasedOn="{StaticResource ListViewStretchedItemContainerStyle}"
        TargetType="ListViewItem">
        <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

</ResourceDictionary>