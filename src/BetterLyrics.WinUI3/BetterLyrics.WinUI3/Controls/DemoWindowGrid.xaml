<?xml version="1.0" encoding="utf-8" ?>

<UserControl
    x:Class="BetterLyrics.WinUI3.Controls.DemoWindowGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource AcrylicBackgroundFillColorBaseBrush}" CornerRadius="4">
        <Border Margin="16">
            <Viewbox
                Width="200"
                Height="100"
                Stretch="Uniform">
                <Grid
                    Width="{x:Bind LyricsWindowStatus.MonitorBounds.Width, Mode=OneWay}"
                    Height="{x:Bind LyricsWindowStatus.MonitorBounds.Height, Mode=OneWay}"
                    Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
                    CornerRadius="32">
                    <Grid
                        Width="{x:Bind LyricsWindowStatus.WindowBounds.Width, Mode=OneWay}"
                        Height="{x:Bind LyricsWindowStatus.WindowBounds.Height, Mode=OneWay}"
                        Margin="{x:Bind LyricsWindowStatus.DemoWindowMargin, Converter={StaticResource AppThicknessToThicknessConverter}, Mode=OneWay}"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Background="{ThemeResource AccentAcrylicBackgroundFillColorDefaultBrush}"
                        CornerRadius="32" />
                </Grid>
            </Viewbox>
        </Border>
        <!--  Is default  -->
        <Grid
            Margin="4"
            Padding="6,3"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Background="{ThemeResource AcrylicBackgroundFillColorBaseBrush}"
            CornerRadius="4"
            Opacity="0.7"
            Visibility="{x:Bind LyricsWindowStatus.IsDefault, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
            <TextBlock
                x:Uid="DemoWindowControlDefault"
                FontSize="12"
                TextWrapping="Wrap" />
        </Grid>
        <!--  Is always on top  -->
        <Grid
            Margin="4"
            Padding="6,3"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Background="{ThemeResource AcrylicBackgroundFillColorBaseBrush}"
            CornerRadius="4"
            Opacity="0.7"
            Visibility="{x:Bind LyricsWindowStatus.IsAlwaysOnTop, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
            <FontIcon
                FontFamily="{StaticResource SegoeFluentIcons}"
                FontSize="12"
                Glyph="&#xE718;" />
            <FontIcon
                FontFamily="{StaticResource SegoeFluentIcons}"
                FontSize="12"
                Glyph="&#xE841;"
                Visibility="{x:Bind LyricsWindowStatus.IsAlwaysOnTopPolling, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
        </Grid>
        <!--  Is locked  -->
        <Grid
            Margin="4"
            Padding="6,3"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Background="{ThemeResource AcrylicBackgroundFillColorBaseBrush}"
            CornerRadius="4"
            Opacity="0.7"
            Visibility="{x:Bind LyricsWindowStatus.IsLocked, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
            <FontIcon
                FontFamily="{StaticResource SegoeFluentIcons}"
                FontSize="12"
                Glyph="&#xE72E;" />
        </Grid>

        <!--  命令区域  -->
        <Grid
            Padding="6"
            Background="{ThemeResource AcrylicInAppFillColorDefaultBrush}"
            Opacity="0"
            RowSpacing="6">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.OpacityTransition>
                <ScalarTransition />
            </Grid.OpacityTransition>
            <interactivity:Interaction.Behaviors>
                <interactivity:EventTriggerBehavior EventName="PointerEntered">
                    <interactivity:ChangePropertyAction PropertyName="Opacity" Value="1" />
                </interactivity:EventTriggerBehavior>
                <interactivity:EventTriggerBehavior EventName="PointerExited">
                    <interactivity:ChangePropertyAction PropertyName="Opacity" Value="0" />
                </interactivity:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
            <Grid
                Grid.Row="0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                ColumnSpacing="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button
                    Grid.Column="0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Click="OpenButton_Click"
                    Content="{ui:FontIcon FontFamily={StaticResource SegoeFluentIcons},
                                          FontSize=16,
                                          Glyph=&#xE78B;}">
                    <ToolTipService.ToolTip>
                        <TextBlock x:Uid="SystemTrayLyrics" />
                    </ToolTipService.ToolTip>
                </Button>
                <Button
                    Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Click="CloseButton_Click"
                    Content="{ui:FontIcon FontFamily={StaticResource SegoeFluentIcons},
                                          FontSize=16,
                                          Glyph=&#xE711;}"
                    IsEnabled="{x:Bind LyricsWindowStatus.WindowStatus, Mode=OneWay, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=0}">
                    <ToolTipService.ToolTip>
                        <TextBlock x:Uid="SettingsPageCloseStatus" />
                    </ToolTipService.ToolTip>
                </Button>
            </Grid>
            <Grid Grid.Row="1" ColumnSpacing="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ToggleButton
                    Grid.Column="0"
                    HorizontalAlignment="Stretch"
                    Content="{ui:FontIcon FontSize=16,
                                          FontFamily={StaticResource SegoeFluentIcons},
                                          Glyph=&#xE80F;}"
                    IsChecked="{x:Bind LyricsWindowStatus.IsDefault, Mode=TwoWay}">
                    <ToolTipService.ToolTip>
                        <TextBlock x:Uid="LyricsWindowSettingsControlSetDefault" />
                    </ToolTipService.ToolTip>
                </ToggleButton>
                <ToggleButton
                    Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    Content="{ui:FontIcon FontSize=16,
                                          FontFamily={StaticResource SegoeFluentIcons},
                                          Glyph=&#xE72E;}"
                    IsChecked="{x:Bind LyricsWindowStatus.IsLocked, Mode=TwoWay}" />
                <ToggleButton
                    Grid.Column="2"
                    HorizontalAlignment="Stretch"
                    IsChecked="{x:Bind LyricsWindowStatus.IsAlwaysOnTop, Mode=TwoWay}">
                    <Grid>
                        <FontIcon
                            FontFamily="{StaticResource SegoeFluentIcons}"
                            FontSize="16"
                            Glyph="&#xE840;" />
                        <FontIcon
                            FontFamily="{StaticResource SegoeFluentIcons}"
                            FontSize="16"
                            Glyph="&#xE841;" />
                    </Grid>
                </ToggleButton>
            </Grid>
        </Grid>

        <Border
            BorderBrush="{ThemeResource AccentAAFillColorDefaultBrush}"
            BorderThickness="2"
            CornerRadius="4"
            Visibility="{x:Bind LyricsWindowStatus.WindowStatus, Mode=OneWay, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=0}" />
    </Grid>

</UserControl>