<UserControl
    x:Class="BetterLyrics.WinUI3.Controls.MediaSettingsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:settings="using:BetterLyrics.Core.Models.Settings"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">

    <Grid>
        <ScrollViewer Style="{StaticResource SettingsScrollViewerStyle}">
            <Grid Style="{StaticResource SettingsGridStyle}">
                <StackPanel>

                    <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" />

                    <controls:SettingsCard x:Uid="SettingsPageMusicLib" HeaderIcon="{ui:FontIcon FontFamily={StaticResource SegoeFluentIcons}, Glyph=&#xE8B7;}" />

                    <InfoBar
                        x:Uid="SettingsPageRemoveInfo"
                        IsClosable="False"
                        IsOpen="True"
                        Severity="Success">
                        <interactivity:Interaction.Behaviors>
                            <interactivity:DataTriggerBehavior
                                Binding="{x:Bind ViewModel.AppSettings.LocalMediaFolders.Count, Mode=OneWay}"
                                ComparisonCondition="Equal"
                                Value="0">
                                <interactivity:ChangePropertyAction PropertyName="Visibility" Value="Collapsed" />
                            </interactivity:DataTriggerBehavior>
                            <interactivity:DataTriggerBehavior
                                Binding="{x:Bind ViewModel.AppSettings.LocalMediaFolders.Count, Mode=OneWay}"
                                ComparisonCondition="NotEqual"
                                Value="0">
                                <interactivity:ChangePropertyAction PropertyName="Visibility" Value="Visible" />
                            </interactivity:DataTriggerBehavior>
                        </interactivity:Interaction.Behaviors>
                    </InfoBar>

                    <ListView
                        ItemContainerStyle="{ThemeResource ListViewStretchedItemContainerStyle}"
                        ItemsSource="{x:Bind ViewModel.AppSettings.LocalMediaFolders, Mode=OneWay}"
                        SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="settings:MediaFolder">
                                <controls:SettingsExpander IsExpanded="True">

                                    <controls:SettingsExpander.HeaderIcon>
                                        <FontIcon FontFamily="{StaticResource SegoeFluentIcons}" Glyph="{x:Bind SourceType, Converter={StaticResource FileSourceTypeToIconConverter}, Mode=OneWay}" />
                                    </controls:SettingsExpander.HeaderIcon>
                                    <controls:SettingsExpander.Header>
                                        <TextBlock IsTextSelectionEnabled="True" Text="{x:Bind Name, Mode=OneWay}" />
                                    </controls:SettingsExpander.Header>
                                    <controls:SettingsExpander.Description>
                                        <TextBlock IsTextSelectionEnabled="True" Text="{x:Bind ConnectionSummary, Mode=OneWay}" />
                                    </controls:SettingsExpander.Description>

                                    <ToggleSwitch IsOn="{x:Bind IsEnabled, Mode=TwoWay}" />

                                    <controls:SettingsExpander.Items>
                                        <controls:SettingsCard x:Uid="MediaSettingsControlNameSetting">
                                            <TextBox VerticalAlignment="Center" Text="{x:Bind Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                        </controls:SettingsCard>
                                        <controls:SettingsCard x:Uid="MediaSettingsControlLastSyncTime" Description="{x:Bind LastSyncTime.ToString(), Mode=OneWay, TargetNullValue=N/A}">
                                            <StackPanel>
                                                <SplitButton
                                                    x:Uid="MediaSettingsControlSyncNow"
                                                    Click="SyncNowButton_Click"
                                                    IsEnabled="{x:Bind IsEnabled, Mode=OneWay}">
                                                    <SplitButton.Flyout>
                                                        <Flyout ShouldConstrainToRootBounds="False">
                                                            <StackPanel MaxWidth="250" Spacing="10">
                                                                <TextBlock x:Uid="MediaSettingsControlAdvOpt" />

                                                                <Button
                                                                    x:Uid="MediaSettingsForceSyncButton"
                                                                    HorizontalAlignment="Stretch"
                                                                    Click="ForceSyncButton_Click" />

                                                                <TextBlock
                                                                    x:Uid="MediaSettingsForceSyncHint"
                                                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                    Style="{ThemeResource CaptionTextBlockStyle}"
                                                                    TextWrapping="Wrap" />
                                                            </StackPanel>
                                                        </Flyout>
                                                    </SplitButton.Flyout>
                                                </SplitButton>
                                            </StackPanel>
                                        </controls:SettingsCard>
                                        <controls:SettingsCard x:Uid="MusicSettingsControlAutoSyncInterval">
                                            <ComboBox IsEnabled="{x:Bind IsEnabled, Mode=OneWay}" SelectedIndex="{x:Bind ScanInterval, Mode=TwoWay, Converter={StaticResource EnumToIntConverter}}">
                                                <ComboBoxItem x:Uid="MusicSettingsControlAutoSyncIntervalDisabled" />
                                                <ComboBoxItem x:Uid="MusicSettingsControlAutoSyncIntervalEveryFifteenMin" />
                                                <ComboBoxItem x:Uid="MusicSettingsControlAutoSyncIntervalEveryHour" />
                                                <ComboBoxItem x:Uid="MusicSettingsControlAutoSyncIntervalEverySixHrs" />
                                                <ComboBoxItem x:Uid="MusicSettingsControlAutoSyncIntervalEveryDay" />
                                            </ComboBox>
                                        </controls:SettingsCard>
                                        <controls:SettingsCard x:Uid="MusicSettingsControlRealTimeScan" IsEnabled="{x:Bind IsLocal, Mode=OneWay}">
                                            <ToggleSwitch IsOn="{x:Bind IsRealTimeScanEnabled, Mode=TwoWay}" />
                                        </controls:SettingsCard>
                                        <controls:SettingsCard>
                                            <Button x:Uid="SettingsPageRemovePath" Click="SettingsPageRemovePathButton_Click" />
                                        </controls:SettingsCard>
                                    </controls:SettingsExpander.Items>

                                    <controls:SettingsExpander.ItemsHeader>
                                        <StackPanel>
                                            <!--  Index info  -->
                                            <InfoBar
                                                BorderThickness="0"
                                                IsClosable="False"
                                                IsOpen="True"
                                                Message="{x:Bind StatusText, Mode=OneWay}"
                                                Severity="{x:Bind StatusSeverity, Converter={StaticResource MessageSeverityToInfoBarSeverity}, Mode=OneWay}" />
                                            <ProgressBar
                                                Background="Transparent"
                                                Visibility="{x:Bind IsProcessing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                                Value="{x:Bind IndexingProgress, Mode=OneWay}">
                                                <interactivity:Interaction.Behaviors>
                                                    <interactivity:DataTriggerBehavior
                                                        Binding="{x:Bind IndexingProgress, Mode=OneWay}"
                                                        ComparisonCondition="Equal"
                                                        Value="0">
                                                        <interactivity:ChangePropertyAction PropertyName="IsIndeterminate" Value="True" />
                                                    </interactivity:DataTriggerBehavior>
                                                    <interactivity:DataTriggerBehavior
                                                        Binding="{x:Bind IndexingProgress, Mode=OneWay}"
                                                        ComparisonCondition="NotEqual"
                                                        Value="0">
                                                        <interactivity:ChangePropertyAction PropertyName="IsIndeterminate" Value="False" />
                                                    </interactivity:DataTriggerBehavior>
                                                </interactivity:Interaction.Behaviors>
                                            </ProgressBar>
                                        </StackPanel>
                                    </controls:SettingsExpander.ItemsHeader>

                                </controls:SettingsExpander>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <StackPanel
                        Margin="0,6,2,0"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal"
                        Spacing="6">
                        <Button Command="{x:Bind ViewModel.NavigationService.OpenMusicGalleryWindowCommand}">
                            <TextBlock x:Uid="SystemTrayMusicGallery" />
                        </Button>
                        <DropDownButton x:Uid="SettingsPageAddFolderButton">
                            <DropDownButton.Flyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem
                                        x:Uid="SettingsPageLocalFolder"
                                        Command="{x:Bind ViewModel.AddMediaSourceCommand}"
                                        CommandParameter="Local">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon FontFamily="{StaticResource SegoeFluentIcons}" Glyph="&#xE8B7;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>

                                    <MenuFlyoutSeparator />

                                    <MenuFlyoutItem
                                        Command="{x:Bind ViewModel.AddMediaSourceCommand}"
                                        CommandParameter="SMB"
                                        Text="SMB">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon FontFamily="{StaticResource SegoeFluentIcons}" Glyph="&#xE839;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>

                                    <MenuFlyoutItem
                                        Command="{x:Bind ViewModel.AddMediaSourceCommand}"
                                        CommandParameter="FTP"
                                        Text="FTP">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon FontFamily="{StaticResource SegoeFluentIcons}" Glyph="&#xE838;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>

                                    <MenuFlyoutItem
                                        Command="{x:Bind ViewModel.AddMediaSourceCommand}"
                                        CommandParameter="WebDAV"
                                        Text="WebDAV">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon FontFamily="{StaticResource SegoeFluentIcons}" Glyph="&#xE774;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>

                                </MenuFlyout>
                            </DropDownButton.Flyout>
                        </DropDownButton>
                    </StackPanel>

                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>