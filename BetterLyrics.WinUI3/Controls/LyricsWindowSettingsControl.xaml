<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="BetterLyrics.WinUI3.Controls.LyricsWindowSettingsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:constants="using:BetterLyrics.WinUI3.Constants"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uc="using:BetterLyrics.WinUI3.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    Loaded="UserControl_Loaded"
    mc:Ignorable="d">

    <Grid>

        <Grid
            x:Name="DisplayGrid"
            Opacity="{x:Bind ViewModel.IsConfigPanelOpened, Mode=OneWay, Converter={StaticResource BoolNegationToOpacityConverter}}"
            SizeChanged="DisplayGrid_SizeChanged">
            <Grid.OpacityTransition>
                <ScalarTransition />
            </Grid.OpacityTransition>
            <ScrollViewer Style="{StaticResource SettingsScrollViewerStyle}">
                <Grid Style="{StaticResource SettingsGridStyle}">

                    <StackPanel Spacing="{StaticResource SettingsCardSpacing}">

                        <TextBlock x:Uid="SettingsPageRecordedWindowStatus" Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" />

                        <StackPanel Orientation="Horizontal" Spacing="3">

                            <!--  Create from templates  -->
                            <Button Content="{ui:FontIcon FontSize=16, FontFamily={StaticResource SegoeFluentIcons}, Glyph=&#xE710;}" Style="{ThemeResource GhostButtonStyle}">
                                <ToolTipService.ToolTip>
                                    <ToolTip x:Uid="SettingsPageCreateFromTemplates" />
                                </ToolTipService.ToolTip>
                                <Button.Flyout>
                                    <MenuFlyout>
                                        <MenuFlyoutItem x:Uid="SettingsPageStandardMode" Command="{x:Bind ViewModel.CreateStandardLyricsWindowStatusCommand}" />
                                        <MenuFlyoutItem x:Uid="SettingsPageDesktopMode" Command="{x:Bind ViewModel.CreateTransparentLyricsWindowStatusCommand}" />
                                        <MenuFlyoutItem x:Uid="SettingsPageDockedMode" Command="{x:Bind ViewModel.CreateDockedLyricsWindowStatusCommand}" />
                                        <MenuFlyoutItem x:Uid="SettingsPageFullscreenMode" Command="{x:Bind ViewModel.CreateFullLyricsWindowStatusCommand}" />
                                        <MenuFlyoutItem x:Uid="SettingsPageNarrowMode" Command="{x:Bind ViewModel.CreateNarrowLyricsWindowStatusCommand}" />
                                        <MenuFlyoutItem x:Uid="SettingsPageTaskbarMode" Command="{x:Bind ViewModel.CreateTaskbarLyricsWindowStatusCommand}" />
                                        <MenuFlyoutItem x:Uid="SettingsPageWallpaperMode" Command="{x:Bind ViewModel.CreateWallpaperLyricsWindowStatusCommand}" />
                                    </MenuFlyout>
                                </Button.Flyout>
                            </Button>

                            <!--  Import  -->
                            <Button
                                Click="ImportButton_Click"
                                Content="{ui:FontIcon FontFamily={StaticResource SegoeFluentIcons},
                                                      FontSize=16,
                                                      Glyph=&#xE74B;}"
                                Style="{ThemeResource GhostButtonStyle}">
                                <ToolTipService.ToolTip>
                                    <ToolTip x:Uid="SettingsPageImport" />
                                </ToolTipService.ToolTip>
                            </Button>

                            <!--  Sharing hub  -->
                            <HyperlinkButton x:Uid="SettingsPageShareHub" NavigateUri="{x:Bind constants:Link.ShareHub}" />

                        </StackPanel>

                        <ListView
                            x:Name="WindowStatusListView"
                            Padding="0,12"
                            HorizontalAlignment="Center"
                            AllowDrop="True"
                            CanDragItems="True"
                            CanReorderItems="True"
                            CornerRadius="4"
                            DragItemsCompleted="WindowStatusListView_DragItemsCompleted"
                            ItemsSource="{x:Bind ViewModel.AppSettings.WindowBoundsRecords, Mode=OneWay}"
                            SelectionMode="None">
                            <ListView.ItemContainerStyle>
                                <Style BasedOn="{StaticResource ListViewCardStyle}" TargetType="ListViewItem">
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <controls:WrapPanel
                                        HorizontalSpacing="8"
                                        Orientation="Horizontal"
                                        VerticalSpacing="8" />
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid Grid.Column="0">
                                            <FontIcon
                                                FontFamily="Segoe UI Symbol"
                                                FontSize="16"
                                                Glyph="&#x283F;" />
                                            <uc:DragIndicator Width="32" />
                                        </Grid>
                                        <StackPanel Grid.Column="1" RightTapped="StackPanel_RightTapped">
                                            <StackPanel.ContextFlyout>
                                                <MenuBarItemFlyout>
                                                    <MenuFlyoutItem x:Uid="SettingsPageCreateFromCurrent" Click="CopyMenuFlyoutItem_Click" />
                                                    <MenuFlyoutSubItem x:Uid="SettingsPageCreateFromCurrentAndTransformTo">
                                                        <MenuFlyoutItem
                                                            x:Uid="SettingsPageStandardMode"
                                                            Click="CopyAndTransformMenuFlyoutItem_Click"
                                                            Tag="StandardMode" />
                                                        <MenuFlyoutItem
                                                            x:Uid="SettingsPageDesktopMode"
                                                            Click="CopyAndTransformMenuFlyoutItem_Click"
                                                            Tag="DesktopMode" />
                                                        <MenuFlyoutItem
                                                            x:Uid="SettingsPageDockedMode"
                                                            Click="CopyAndTransformMenuFlyoutItem_Click"
                                                            Tag="DockedMode" />
                                                        <MenuFlyoutItem
                                                            x:Uid="SettingsPageFullscreenMode"
                                                            Click="CopyAndTransformMenuFlyoutItem_Click"
                                                            Tag="FullscreenMode" />
                                                        <MenuFlyoutItem
                                                            x:Uid="SettingsPageNarrowMode"
                                                            Click="CopyAndTransformMenuFlyoutItem_Click"
                                                            Tag="NarrowMode" />
                                                        <MenuFlyoutItem
                                                            x:Uid="SettingsPageTaskbarMode"
                                                            Click="CopyAndTransformMenuFlyoutItem_Click"
                                                            Tag="TaskbarMode" />
                                                        <MenuFlyoutItem
                                                            x:Uid="SettingsPageWallpaperMode"
                                                            Click="CopyAndTransformMenuFlyoutItem_Click"
                                                            Tag="WallpaperMode" />
                                                    </MenuFlyoutSubItem>
                                                    <MenuFlyoutItem x:Uid="LyricsWindowSettingsControlShare" Click="ShareMenuFlyoutItem_Click" />
                                                    <MenuFlyoutItem
                                                        x:Uid="SettingsPageDelete"
                                                        Click="DeleteMenuFlyoutItem_Click"
                                                        IsEnabled="{Binding IsDefault, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}" />
                                                </MenuBarItemFlyout>
                                            </StackPanel.ContextFlyout>
                                            <uc:DemoWindowGrid LyricsWindowStatus="{Binding}" />
                                            <Button HorizontalAlignment="Stretch" Click="ConfigButton_Click">
                                                <TextBlock x:Uid="LyricsWindowSettingsControlLyricsWindowConfig" />
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <controls:SettingsCard x:Uid="SettingsPageMultiNowPlayingWindows">
                            <ToggleSwitch IsOn="{x:Bind ViewModel.AppSettings.GeneralSettings.MultiNowPlayingWindowMode, Mode=TwoWay}" />
                        </controls:SettingsCard>

                        <controls:SettingsCard x:Uid="SettingsPageMusicGalleryLyrics">
                            <Button HorizontalAlignment="Stretch" Click="EmbeddedConfigButton_Click">
                                <TextBlock x:Uid="LyricsWindowSettingsControlLyricsWindowConfig" />
                            </Button>
                        </controls:SettingsCard>

                    </StackPanel>

                </Grid>
            </ScrollViewer>
        </Grid>

        <Grid
            x:Name="ConfigGrid"
            Opacity="{x:Bind ViewModel.IsConfigPanelOpened, Mode=OneWay, Converter={StaticResource BoolToOpacityConverter}}"
            Translation="{x:Bind ViewModel.ConfigPanelTranslation, Mode=OneWay}">
            <Grid.OpacityTransition>
                <ScalarTransition />
            </Grid.OpacityTransition>
            <Grid.TranslationTransition>
                <Vector3Transition />
            </Grid.TranslationTransition>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid
                Grid.Row="0"
                Padding="36,0"
                RowSpacing="{StaticResource SettingsCardSpacing}"
                Style="{StaticResource SettingsGridStyle}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        Grid.Column="0"
                        Style="{StaticResource SettingsSectionHeaderTextBlockStyle}"
                        Text="{x:Bind LyricsWindowStatus.Name, Mode=OneWay}" />
                    <Button
                        Grid.Column="2"
                        Margin="0,24,0,0"
                        Command="{x:Bind ViewModel.CloseConfigPanelCommand}"
                        Content="{ui:FontIcon FontSize=16,
                                              FontFamily={StaticResource SegoeFluentIcons},
                                              Glyph=&#xE70D;}"
                        Style="{ThemeResource AccentButtonStyle}" />
                </Grid>

                <NavigationView
                    x:Name="ConfigNavView"
                    Grid.Row="1"
                    IsBackButtonVisible="Collapsed"
                    IsBackEnabled="False"
                    IsSettingsVisible="False"
                    PaneDisplayMode="Top"
                    SelectionChanged="ConfigNavView_SelectionChanged">
                    <NavigationView.MenuItems>
                        <NavigationViewItem x:Name="WindowSegmentedItem" Tag="Window">
                            <TextBlock x:Uid="AppSettingsControlGeneral" />
                        </NavigationViewItem>
                        <NavigationViewItem Tag="AlbumArtStyle">
                            <TextBlock x:Uid="SettingsPageAlbumStyle" />
                        </NavigationViewItem>
                        <NavigationViewItem Tag="AlbumArtEffect">
                            <TextBlock x:Uid="SettingsPageAlbumEffect" />
                        </NavigationViewItem>
                        <NavigationViewItem Tag="LyricsStyle">
                            <TextBlock x:Uid="SettingsPageLyricsStyle" />
                        </NavigationViewItem>
                        <NavigationViewItem Tag="LyricsEffect">
                            <TextBlock x:Uid="SettingsPageLyricsEffect" />
                        </NavigationViewItem>
                        <NavigationViewItem Tag="LyricsBackground">
                            <TextBlock x:Uid="SettingsPageBackgroundOverlay" />
                        </NavigationViewItem>
                    </NavigationView.MenuItems>
                    <!--<Frame x:Name="ConfigFrame" />-->
                </NavigationView>
            </Grid>

            <controls:SwitchPresenter Grid.Row="1" Value="{x:Bind ViewModel.SelectorBarSelectedItemTag, Mode=OneWay}">
                <controls:SwitchPresenter.ContentTransitions>
                    <TransitionCollection>
                        <PopupThemeTransition />
                    </TransitionCollection>
                </controls:SwitchPresenter.ContentTransitions>

                <!--  Window  -->
                <controls:Case Value="Window">
                    <uc:WindowSettingsControl LyricsWindowStatus="{x:Bind LyricsWindowStatus, Mode=OneWay}" />
                </controls:Case>

                <!--  Album art area style  -->
                <controls:Case Value="AlbumArtStyle">
                    <uc:AlbumArtAreaStyleSettingsControl AlbumArtLayoutSettings="{x:Bind LyricsWindowStatus.AlbumArtLayoutSettings, Mode=OneWay}" />
                </controls:Case>

                <!--  Album art area effect  -->
                <controls:Case Value="AlbumArtEffect">
                    <uc:AlbumArtAreaEffectSettingsControl AlbumArtAreaEffectSettings="{x:Bind LyricsWindowStatus.AlbumArtAreaEffectSettings, Mode=OneWay}" />
                </controls:Case>

                <!--  Lyrics style  -->
                <controls:Case Value="LyricsStyle">
                    <uc:LyricsStyleSettingsControl LyricsStyleSettings="{x:Bind LyricsWindowStatus.LyricsStyleSettings, Mode=OneWay}" />
                </controls:Case>

                <!--  Lyrics  effect  -->
                <controls:Case Value="LyricsEffect">
                    <uc:LyricsEffectSettingsControl LyricsEffectSettings="{x:Bind LyricsWindowStatus.LyricsEffectSettings, Mode=OneWay}" />
                </controls:Case>

                <!--  Lyrics background  -->
                <controls:Case Value="LyricsBackground">
                    <uc:LyricsBackgroundSettingsControl LyricsBackgroundSettings="{x:Bind LyricsWindowStatus.LyricsBackgroundSettings, Mode=OneWay}" />
                </controls:Case>

            </controls:SwitchPresenter>

        </Grid>

    </Grid>
</UserControl>
