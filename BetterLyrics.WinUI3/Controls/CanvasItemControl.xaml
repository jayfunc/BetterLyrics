<UserControl
    x:Class="BetterLyrics.WinUI3.Controls.CanvasItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="SizingWrapper">
        <Border
            x:Name="MainBorder"
            x:FieldModifier="public"
            CanDrag="True"
            CornerRadius="4">
            <TextBlock
                x:Name="TitleBlock"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Foreground="White" />
        </Border>

        <Border
            x:Name="RightHandle"
            Width="12"
            Height="12"
            Margin="0,0,-4,0"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            x:FieldModifier="public"
            Background="White"
            BorderBrush="{Binding BorderBrush, ElementName=MainBorder, Mode=OneWay}"
            BorderThickness="2"
            CornerRadius="6" />

        <Border
            x:Name="BottomHandle"
            Width="12"
            Height="12"
            Margin="0,0,0,-4"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            x:FieldModifier="public"
            Background="White"
            BorderBrush="{Binding BorderBrush, ElementName=MainBorder, Mode=OneWay}"
            BorderThickness="2"
            CornerRadius="6" />

        <Border
            x:Name="CornerHandle"
            Width="12"
            Height="12"
            Margin="0,0,-4,-4"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            x:FieldModifier="public"
            Background="White"
            BorderBrush="{Binding BorderBrush, ElementName=MainBorder, Mode=OneWay}"
            BorderThickness="2"
            CornerRadius="6" />
    </Grid>
</UserControl>