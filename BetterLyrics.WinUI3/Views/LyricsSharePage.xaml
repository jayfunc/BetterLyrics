<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="BetterLyrics.WinUI3.Views.LyricsSharePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:local="using:BetterLyrics.WinUI3.Views"
    xmlns:lyricsmodels="using:BetterLyrics.WinUI3.Models.Lyrics"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:BetterLyrics.WinUI3.Models"
    xmlns:uc="using:BetterLyrics.WinUI3.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:viewmodels="using:BetterLyrics.WinUI3.ViewModels"
    mc:Ignorable="d">

    <Page.Resources>
        <CollectionViewSource
            x:Name="StyleGroupsCVS"
            IsSourceGrouped="True"
            Source="{x:Bind ViewModel.StyleGroups, Mode=OneWay}" />
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="LeftColDef" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition x:Name="RightColDef" />
        </Grid.ColumnDefinitions>

        <Grid
            Grid.Column="0"
            Padding="24"
            RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Spacing="12">
                <TextBlock x:Uid="LyricsSharePageSelectLyricsTitle" Style="{ThemeResource SubtitleTextBlockStyle}" />
                <CheckBox
                    x:Name="LyricsHostCheckBox"
                    Click="LyricsHostCheckBox_Click"
                    IsThreeState="True" />
            </StackPanel>
            <ListView
                x:Name="LyricsListView"
                Grid.Row="1"
                ItemsSource="{x:Bind ViewModel.GSMTCService.CurrentLyricsData.LyricsLines, Mode=OneWay}"
                SelectionChanged="LyricsListView_SelectionChanged"
                SelectionMode="Multiple">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="lyricsmodels:LyricsLine">
                        <StackPanel Padding="0,8" Spacing="2">
                            <TextBlock Text="{x:Bind Text}" TextWrapping="Wrap" />
                            <TextBlock
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="{x:Bind SecondaryText}"
                                TextWrapping="Wrap"
                                Visibility="{x:Bind SecondaryText, Mode=OneWay, Converter={StaticResource StringToVisibilityConverter}}" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <Grid
            x:Name="RootGrid"
            Grid.Column="1"
            Margin="24"
            VerticalAlignment="Center"
            RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Viewbox x:Name="PreviewCardContainer">
                <ContentControl
                    x:Name="PreviewCard"
                    Content="{x:Bind ViewModel.CardData, Mode=OneWay}"
                    ContentTemplate="{x:Bind ViewModel.CardDataTemplate, Mode=OneWay}" />
            </Viewbox>
            <TextBlock
                Grid.Row="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind ViewModel.SelectedStyleItem.DisplayText, Mode=OneWay}" />
        </Grid>

        <Grid
            Grid.Column="2"
            Padding="24"
            RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <NavigationView
                    x:Name="ConfigNavView"
                    Grid.Row="0"
                    Margin="-16,-8,0,0"
                    IsBackButtonVisible="Collapsed"
                    IsBackEnabled="False"
                    IsSettingsVisible="False"
                    PaneDisplayMode="Top"
                    SelectionChanged="ConfigNavView_SelectionChanged">
                    <NavigationView.MenuItems>
                        <NavigationViewItem IsSelected="True" Tag="Style">
                            <TextBlock x:Uid="LyricsSharePageStyleTitle" />
                        </NavigationViewItem>
                        <NavigationViewItem Tag="FontFamily">
                            <TextBlock x:Uid="LyricsSharePageFontFamily" />
                        </NavigationViewItem>
                        <NavigationViewItem Tag="ImageQuality">
                            <TextBlock x:Uid="LyricsSharePageImageQuality" />
                        </NavigationViewItem>
                    </NavigationView.MenuItems>
                </NavigationView>
            </Grid>
            <controls:SwitchPresenter Grid.Row="1" Value="{x:Bind ViewModel.ConfigNavViewSelectedItemTag, Mode=OneWay}">
                <controls:SwitchPresenter.ContentTransitions>
                    <TransitionCollection>
                        <PopupThemeTransition />
                    </TransitionCollection>
                </controls:SwitchPresenter.ContentTransitions>

                <controls:Case Value="Style">
                    <SemanticZoom>
                        <SemanticZoom.ZoomedInView>
                            <GridView ItemsSource="{Binding Source={StaticResource StyleGroupsCVS}}" SelectedItem="{x:Bind ViewModel.SelectedStyleItem, Mode=TwoWay}">

                                <GridView.ItemContainerStyle>
                                    <Style BasedOn="{StaticResource DefaultGridViewItemStyle}" TargetType="GridViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                        <Setter Property="VerticalContentAlignment" Value="Center" />
                                    </Style>
                                </GridView.ItemContainerStyle>

                                <GridView.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <ItemsWrapGrid HorizontalAlignment="Stretch" Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </GridView.ItemsPanel>

                                <GridView.GroupStyle>
                                    <GroupStyle>

                                        <GroupStyle.HeaderContainerStyle>
                                            <Style TargetType="GridViewHeaderItem">
                                                <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
                                                <Setter Property="FontSize" Value="{ThemeResource GridViewHeaderItemThemeFontSize}" />
                                                <Setter Property="Background" Value="Transparent" />
                                                <Setter Property="Margin" Value="0,0,0,4" />
                                                <Setter Property="Padding" Value="6,12" />
                                                <Setter Property="HorizontalContentAlignment" Value="Left" />
                                                <Setter Property="VerticalContentAlignment" Value="Top" />
                                                <Setter Property="MinHeight" Value="0" />
                                                <Setter Property="CornerRadius" Value="4" />
                                                <Setter Property="UseSystemFocusVisuals" Value="{StaticResource UseSystemFocusVisuals}" />
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="GridViewHeaderItem">
                                                            <Grid
                                                                x:Name="RootGrid"
                                                                Background="{TemplateBinding Background}"
                                                                CornerRadius="{TemplateBinding CornerRadius}">
                                                                <ContentPresenter
                                                                    x:Name="ContentPresenter"
                                                                    Margin="{TemplateBinding Padding}"
                                                                    HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                    VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                                                    Content="{TemplateBinding Content}"
                                                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                    ContentTransitions="{TemplateBinding ContentTransitions}" />
                                                                <VisualStateManager.VisualStateGroups>
                                                                    <VisualStateGroup x:Name="CommonStates">
                                                                        <VisualStateGroup.Transitions>
                                                                            <VisualTransition GeneratedDuration="0:0:0.1" />
                                                                        </VisualStateGroup.Transitions>

                                                                        <VisualState x:Name="Normal" />

                                                                        <VisualState x:Name="PointerOver">
                                                                            <VisualState.Setters>
                                                                                <Setter Target="RootGrid.Background" Value="{ThemeResource ListViewItemBackgroundPointerOver}" />
                                                                            </VisualState.Setters>
                                                                        </VisualState>

                                                                        <VisualState x:Name="Pressed">
                                                                            <VisualState.Setters>
                                                                                <Setter Target="RootGrid.Background" Value="{ThemeResource ListViewItemBackgroundPressed}" />
                                                                            </VisualState.Setters>
                                                                        </VisualState>

                                                                        <VisualState x:Name="Disabled">
                                                                            <VisualState.Setters>
                                                                                <Setter Target="ContentPresenter.Opacity" Value="{ThemeResource ListViewItemDisabledThemeOpacity}" />
                                                                            </VisualState.Setters>
                                                                        </VisualState>
                                                                    </VisualStateGroup>
                                                                </VisualStateManager.VisualStateGroups>
                                                            </Grid>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </GroupStyle.HeaderContainerStyle>

                                        <GroupStyle.HeaderTemplate>
                                            <DataTemplate x:DataType="viewmodels:StyleGroup">
                                                <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}" Text="{x:Bind GroupTitle}" />
                                            </DataTemplate>
                                        </GroupStyle.HeaderTemplate>

                                        <GroupStyle.Panel>
                                            <ItemsPanelTemplate>
                                                <controls:WrapPanel
                                                    HorizontalSpacing="8"
                                                    Orientation="Horizontal"
                                                    VerticalSpacing="8" />
                                            </ItemsPanelTemplate>
                                        </GroupStyle.Panel>

                                    </GroupStyle>
                                </GridView.GroupStyle>

                                <GridView.ItemTemplate>
                                    <DataTemplate x:DataType="viewmodels:StyleItem">
                                        <Grid Padding="0" Style="{StaticResource CardGridStyle}">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="*" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <Grid Grid.Row="0" Background="{ThemeResource SmokeFillColorDefaultBrush}">
                                                <Viewbox
                                                    Height="150"
                                                    IsHitTestVisible="False"
                                                    Stretch="Uniform">
                                                    <ContentControl Content="{x:Bind CardData}" ContentTemplate="{x:Bind CardDataTemplate}" />
                                                </Viewbox>
                                            </Grid>
                                            <TextBlock
                                                Grid.Row="1"
                                                Margin="8"
                                                HorizontalAlignment="Center"
                                                Text="{x:Bind DisplayText}"
                                                TextAlignment="Center" />
                                        </Grid>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                            </GridView>
                        </SemanticZoom.ZoomedInView>
                        <SemanticZoom.ZoomedOutView>
                            <ListView
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                ItemsSource="{Binding Source={StaticResource StyleGroupsCVS}, Path=CollectionGroups}">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Margin="0,12"
                                            Style="{ThemeResource SubtitleTextBlockStyle}"
                                            Text="{Binding Group.GroupTitle}" />
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </SemanticZoom.ZoomedOutView>
                    </SemanticZoom>
                </controls:Case>
                <controls:Case Value="FontFamily">
                    <uc:FontPicker
                        Margin="-4,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        SelectedFontId="{x:Bind ViewModel.CardData.Config.FontFamily, Mode=TwoWay}" />
                </controls:Case>
                <controls:Case Value="ImageQuality">
                    <uc:ExtendedSlider
                        x:Name="ImageQualitySlider"
                        HorizontalAlignment="Left"
                        Default="100"
                        Frequency="1"
                        Maximum="100"
                        Minimum="10"
                        Unit="%"
                        Value="100" />
                </controls:Case>

            </controls:SwitchPresenter>
            <Grid Grid.Row="2">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button
                        x:Uid="LyricsSharePageSaveImage"
                        Click="SaveImage_Click"
                        Style="{ThemeResource AccentButtonStyle}" />
                    <Button x:Uid="LyricsSharePageCopyImage" Click="CopyImage_Click" />
                </StackPanel>
            </Grid>
        </Grid>

        <Grid
            x:Name="ProcessingOverlay"
            Grid.ColumnSpan="3"
            Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
            Opacity="0"
            Visibility="Collapsed">
            <Grid.OpacityTransition>
                <ScalarTransition />
            </Grid.OpacityTransition>
            <ProgressRing />
        </Grid>

    </Grid>
</Page>
